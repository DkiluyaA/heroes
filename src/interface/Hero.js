import React from "react";

class Hero{
    constructor(name, description, isClick,url) {
        this.name=name;
        this.description=description;
        this.isClick=isClick;
        this.url=url;
    }
}

export default Hero;

